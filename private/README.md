# Private Artifacts

This directory is ignored except for this README.

Use it for local-only inputs and generated artifacts such as:

- `default.xbe`
- extracted disc files
- Ghidra project directories
- patched test binaries
- xemu logs tied to private paths or data

Do not commit those files.

The public custody check permits this README and rejects every other tracked
path under `private/`.
